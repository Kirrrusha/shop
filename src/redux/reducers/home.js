import { HOME } from '../../constants/redux';

const defaultState = {};

const reducer = (state = defaultState, { type }) => {
    switch (type) {
        default:
            return state;
    }
};

export default reducer;